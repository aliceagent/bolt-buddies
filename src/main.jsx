import React from "react";
import { createRoot } from "react-dom/client";
import StagwellDemo from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <StagwellDemo />
  </React.StrictMode>
);
